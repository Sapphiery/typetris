import React from "react";
import "./style.css";

const Info = props => (
    <div className="Info">
        <h1 className = "title">Instructions:</h1>
        <p className="infoText">Etc...</p>
    </div>
);

export default Info;